attachment points
attachmentPointsSpecs
	^ {
		"{#center. #color. #borderColor}"
		{self center + (-33@123). Color gray. Color gray}.
		{self center + (-20@123). Color gray. Color gray}.
		{self center + (-7@123). Color gray. Color gray}.
		{self center + (6@123). Color gray. Color gray}.
		{self center + (19@123). Color gray. Color gray}.
		{self center + (32@123). Color gray. Color gray}.
	}